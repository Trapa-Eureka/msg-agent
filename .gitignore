# OS
.DS_Store
Thumbs.db

# Node
node_modules/
dist/
coverage/
*.tsbuildinfo
npm-debug.log*

# Secrets — guardrail 4: keys and tokens live only in .env and ~/.msg-agent/config.json (mode 600)
.env
.env.*
!.env.example
# local config dir if someone points --config inside the repo
.msg-agent/

# Editors
.vscode/
.idea/
