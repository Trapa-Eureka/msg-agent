# TASKS — msg-agent v0.1 backlog

## How to use

- One agent session = one task. Prompt template:
  > Read `docs/SPEC.md`, `docs/DESIGN.md` and `docs/TESTING.md`, then carry out **T4**. Keep fixing until every completion criterion is met and `npm run check` passes. When done, summarize the changed files and the verification results.
- Every completion criterion is machine-checkable. On completion, set the status to `DONE(date)` and commit (`T{n}: summary`).
- Parallel lanes: after T1, **A (T2), B (T3), C (T4), D (T5)** can run concurrently in separate worktree agents.

Dependency graph: `T0 → T1 → {A: T2, B: T3, C: T4, D: T5} → T6(T2–T5) → {T7(T6), T8(T6)} → T9(T7,T8) → T10(T9) → T11`

**Status (2026-09-05): T0–T11 all DONE.** Review remediation R1–R7 done on 2026-09-06 → release checklist completed.

---

### T0 — Project scaffolding · status: DONE(2026-09-05)
- Goal: TS strict + ESLint + Prettier + Vitest + scripts (`check/test/typecheck/lint/cli/smoke`), `.env.example`, `.gitignore` (.env; ~/.msg-agent lives in the home directory so it is out of scope).
- Done when: [x] `npm run check` passes [x] one dummy test [x] git init + first commit
- Decisions: placeholder package name `message` + `private: true` (removed in T11) / CLI invocation unified as `npm run cli -- <cmd>` / ESM (`type: module`) + TypeScript pinned to 5.x (typescript-eslint compatibility) / Prettier excludes `*.md` (docs are not touched)

### T1 — Types and config schema · status: DONE(2026-09-05) · depends on: T0
- Goal: `core/types.ts` (all of DESIGN §2), config zod schema + `configStore` (load/save, file mode 600, env-reference resolution).
- Done when: [x] round-trip and error-case tests (missing required field, bad language code → fix guidance) [x] check passes
- Decisions: errors are returned as `Result` + codes (`ConfigIssue`) instead of thrown, wording lives only in `core/configMessages.ts` (ko/en) → later folded into the T8 phrase pack / language-code validation and normalization via the `iso-639-3` table (639-1 preferred) / `.env` loaded without a dependency (`process.loadEnvFile`, later replaced by `util.parseEnv` in R3) / secrets shown only via `redactSecretRef`

### T2 (lane A) — Three extractors · status: DONE(2026-09-05) · depends on: T1
- Goal: pdf-parse, mammoth and UTF-8 extractors + section structuring; produce `fixtures/docs/` (the full TESTING §2 list).
- Done when: [x] happy-path extraction tests per format [x] empty-text and encrypted PDFs return clear failure types [x] check passes
- Decisions: `extract` returns `Result<ExtractedDoc, ExtractError>` (empty_text | encrypted | corrupt) / section structuring is the shared `core/sections.ts` (blank lines, Markdown headings, short unterminated lines) + PDF-specific line recovery (`pdfPagesToText`: lines short relative to the page's longest line = heading or paragraph end; CJK wraps joined without spaces) / DOCX headings converted to `#` Markdown so the same heuristic applies (extended in R6 to keep links, numbered and nested lists and heading levels) / fixtures generated by `scripts/fixtures/generate.ts` (pdfkit, docx, macOS fonts) and committed

### T3 (lane B) — Detection, chunking, planner · status: DONE(2026-09-05) · depends on: T1
- Goal: franc-based detector (with confidence), section-boundary chunker, outputPlanner (smart policy).
- Done when: [x] **all seven TESTING §3 golden plans** [x] RTL/CJK split integrity [x] check passes
- Decisions: confidence is not franc's score gap (it converges to 0 on long text) but sample length × head/tail agreement, threshold 0.7 / `sco` (Scots) ignored as an English shadow / default chunk 4,000 chars, hard split by Intl.Segmenter graphemes / the planner returns only a `PlanDecision`; assembling the real `OutputPlan` is T6

### T4 (lane C) — Translation providers · status: DONE(2026-09-05) · depends on: T1
- Goal: TranslatorProvider interface + Claude and OpenAI adapters (injected fetch), FakeTranslator (markers, failure injection, call counting), translation and summary prompt templates.
- Done when: [x] request shape verified with a mock fetch [x] prompts state "preserve numbers and proper nouns, output the translation only" [x] check passes
- Decisions: Claude uses the official SDK with an injected `fetch` (request shape verified by mock fetch), default `claude-sonnet-5` (owner decision, cost), translation effort low, summary medium, server-side fallbacks / OpenAI uses Chat Completions over raw fetch, default `gpt-5` / errors unified as `ProviderError` (kind, retryable, status, detail = error name only), chunk retries belong to the T6 pipeline / `verify()` is a models lookup that spends no tokens / prompts live only in `core/prompts.ts`

### T5 (lane D) — Telegram adapter + FakeMessenger · status: DONE(2026-09-05) · depends on: T1
- Goal: grammY adapter (document events → IncomingDoc, command routing, 4,096-char split postText, sendDocument), FakeMessenger (same split rule).
- Done when: [x] split-rule unit tests (function shared by adapter and fake) [x] metadata over 20 MB → no download attempted [x] request-shape tests without real calls [x] check passes
- Decisions: one split rule in `core/textSplit.ts` (paragraph → line → sentence → grapheme) shared by adapter and fake / `download()` is lazy and the adapter itself refuses > 20 MB (no getFile) / command autocompletion via `setMyCommands` in `start()` / tests use `botInfo` injection + `api.config.use` transformer + `handleUpdate` + injected fetch for zero network / when posting in parts, only the first part replies to the source message

### T6 — Pipeline · status: DONE(2026-09-05) · depends on: T2, T3, T4, T5
- Goal: `core/pipeline.ts` — the eight steps of DESIGN §3 + command handling (last-document reference in memory, metadata only).
- Done when: [x] **every item of the TESTING §4 checklist** (privacy and cost guards included) [x] check passes
- Decisions: user phrases leave only through the `Phrases` interface (core/phrases.ts, 20 keys), no literals in the core → T8 implements ko/en / settings persistence, logging and the clock are injected as `SettingsStore`, `Logger`, `Clock` ports / translation is one call per chunk + one retry when retryable, nothing posted on failure / per-chat serialization queue / progress notices at most 4 when chunks ≥ 2 / mocks added: FakePhrases (key-tag output), MemorySettings, CapturingLogger, FixedClock, FixtureExtractor (+ syntheticDoc)

### T7 — CLI onboarding and daemon · status: DONE(2026-09-05) · depends on: T6
- Goal: `cli/init` (three interactive questions + immediate key/token verification), `cli/start` (daemon, signal cleanup), `cli/status`.
- Done when: [x] prompt-injected unit tests (non-interactive mode) [x] fix guidance on verification failure [x] check passes
- Decisions: the three commands are `runInit/runStart/runStatus` (dependency-injected) + `cli/index.ts` assembly (commander, prompts, grammY getMe) / keys and tokens: offer an `env:` reference when the variable exists, otherwise store `literal:`; on verification failure show cause + fix and allow up to 3 attempts / screen wording in `cli/text.ts` (ko/en) / adapters added: `FileSettings` (configStore wrapper), `ConsoleLogger` (stderr JSON lines) / phrase pack temporarily `src/phrases/en.ts` only — **T8 adds ko, fallback and snapshots**

### T8 — User phrase pack · status: DONE(2026-09-05) · depends on: T6
- Goal: render user-facing messages (progress, skip, rejection, errors) in the native language — at least ko and en packs + language fallback (en).
- Done when: [x] a missing phrase key fails the build (enforced by types) [x] ko/en snapshot tests [x] check passes
- Decisions: `src/phrases/{ko,en}.ts` use `satisfies Phrases` (verified that removing one key fails compilation) / `phrasesFor` normalizes the ISO spelling and falls back to en / language names rendered per pack language with `Intl.DisplayNames` (the pipeline passes codes only) / snapshot `tests/__snapshots__/phrases.test.ts.snap` (20 keys × 2 languages) / CLI screen wording (`cli/text.ts`) and config error wording (`core/configMessages.ts`) stay separate from the messenger pack

### T9 — e2e-mock · status: DONE(2026-09-05) · depends on: T7, T8
- Goal: verify every SPEC §7 scenario (short / long / skip / four commands / two concurrent chats) with FakeMessenger in the assembled-daemon state.
- Done when: [x] all scenarios pass [x] check passes
- Decisions: `tests/e2e.test.ts` assembles for real through `runStart` (real extractors, franc, ko/en phrase packs, file settings store) and fakes only the messenger and translator / nine scenarios: short PDF inline, long PDF summary + file, Korean skip, scanned/encrypted/unsupported/over-cap guidance, four commands (config persistence, EN wording after a language switch), two concurrent chats, no residue in logs/output/disk, session cost guard / found and fixed: pdf.js detached the input buffer so re-extracting the same bytes failed → PdfExtractor passes a copy

### T10 — Coverage + privacy audit test · status: DONE(2026-09-05) · depends on: T9
- Goal: core ≥ 90% report; an always-on automated check (signature-string technique) that no body text remains in logs or temp files.
- Done when: [x] report attached (`docs/COVERAGE.md`: core 96.9% / lines 97.2%) [x] audit test part of check (`tests/privacy-audit.test.ts`) [x] check passes
- Decisions: the test stage of `npm run check` became `vitest run --coverage` with vitest thresholds (statements/lines/functions 90, branches 80) — verified that a threshold of 99 fails / the audit has two layers: static (disk-write APIs only in configStore, console only in cli, no body keys in logger metadata) + runtime (three signatures, real ConsoleLogger with stdout/stderr spies, success and failure paths, no residue in cwd or the config directory) / the audit test must never be deleted or weakened (WORKFLOW §2)

### T11 — Smoke + release preparation · status: DONE(2026-09-05) · depends on: T10
- Goal: `scripts/smoke.ts` (TESTING §5, including the group privacy-mode check), README quick start with real commands, research and record three npm package-name candidates (publishing needs human approval).
- Done when: [x] smoke checklist output [x] README onboarding ≤ 5 lines (3) [x] name candidates recorded in SPEC §8 [x] check passes
- Decisions: the smoke assembles via `runStart` unchanged and wraps the logger to fill the checklist from pipeline events (doc.received/planned/done) — never prints content / group privacy mode judged from getMe's `can_read_all_group_messages` (no group upload needed) / with `--chat` a guidance message is posted; waits for one document for `--wait` (default 300 s) / candidates: msg-agent · docslate · chatdoc-translate (+ `@shiz_son/msg-agent`) / **running the real smoke, choosing the name and publishing are human tasks (HUMAN_PREP §2, §3)**

---

## Review remediation (R series) · 2026-09-05 code review (`docs/001_CODE_REVIEW.md`) and security review (`docs/002_SECURITY_REVIEW.md`)

Handled before release. One session = one R task; on completion `npm run check` + PR merge. Numbers refer to the items in the review documents.

### R1 — Access control · status: DONE(2026-09-06) · [01, SEC-01, part of SEC-12]
- Goal: deny by default based on owner and allowed chats. Add the sender `userId` to events. Pairing: `start` prints a six-digit code → the owner sends `/start <code>` to the bot → owner registered + that chat allowed. Owner commands `/allow` (allow the current chat) and `/deny`. Documents, `/full`, `/summary` only for allowed chats or the owner; `/mode`, `/lang` owner-only. Denials are silent (metadata log only). Config `access` schema (strict, unknown keys rejected).
- Done when: [x] document from an unauthorized user → 0 downloads [x] non-owner `/mode` ignored [x] pairing and allow/deny e2e [x] check passes
- Decisions: config root, provider, messenger and access are all `strictObject` (unknown key → invalid_value "unknown key") / the pairing code is generated by `start` with crypto.randomInt, printed in the terminal, single-use, valid for the process lifetime / denials are only an `access.denied` warning log (kind, chatId, userId) / FakeMessenger events default to userId "owner", so existing tests run as the owner scenario / **existing config files are unpaired, so `start` must be followed by `/start <code>`**

### R2 — Cost and rate guards · status: DONE(2026-09-06) · [02, 07, 15, SEC-02]
- Goal: measure the cost guard on the sent length (whitespace included), max chunks per document, per-chat documents per hour and a global daily character budget (in memory, metadata only). Claude SDK `maxRetries: 0` (the single retry lives in the pipeline), OpenAI `max_completion_tokens`. A single grapheme above the limit is split by code points to keep the length invariant.
- Done when: [x] whitespace-bypass case rejected (399,400 chars → rejectOverMax) [x] ≤ 2 requests per chunk against a 529 mock (SDK maxRetries 0 → one request per attempt) [x] 4,201-unit grapheme case → every part ≤ 4,096 [x] check passes
- Decisions: cost measure = normalized text `length` (whitespace included) / `maxChunksPerDoc` default 50 (pipeline option) / `limits` (strict) default 20 docs/hour and 1,000,000 chars/day, re-run commands count too, the summary path charges characters twice / over-cap guidance asks to split the file instead of suggesting `/summary` (resolves item 11) / OpenAI `max_completion_tokens` 16000 / a grapheme longer than the limit is split by code points (length invariant over grapheme integrity)

### R3 — Secret and config-file safety · status: DONE(2026-09-06) · [03, 12, SEC-04, SEC-05, SEC-06, SEC-07, SEC-13]
- Goal: never print JSON parser text (fixed code), atomic saves (temp 600 → rename), refuse symlinks and loose permissions on load, load only the three allow-listed `.env` keys, Claude SDK `logLevel: "off"` + pinned official baseURL, `.msg-agent/` in `.gitignore`.
- Done when: [x] zero leakage from malformed JSON containing a signature [x] original preserved when a save fails [x] `ANTHROPIC_BASE_URL` and `ANTHROPIC_LOG` ignored (pinned SDK baseURL, logLevel off, selective .env) [x] check passes
- Decisions: symlink, non-regular file or group/other bits on load → `unreadable` (detail codes: symlink / not_regular_file / insecure_permissions, with fix guidance) — loose permissions are refused, not silently repaired / saves go to a temp file (`wx`, 600) → rename, temp removed on failure / `.env` via `util.parseEnv`, three allow-listed keys only / `.msg-agent/` in `.gitignore`

### R4 — Boundary validation and deadlines · status: DONE(2026-09-06) · [06, 14, part of SEC-03, SEC-08, SEC-09, SEC-12]
- Goal: zod validation of OpenAI responses (→ `bad_response`), Claude response shape check, AbortSignal deadlines on OpenAI fetch and Telegram downloads + a streaming byte cap (including the getFile file_size check), Claude SDK timeout.
- Done when: [x] `null` / numeric content → `bad_response` (zod, five malformed shapes) [x] stream cut off over the cap (getFile file_size, content-length and stream accumulation — three layers) [x] check passes
- Decisions: OpenAI `AbortSignal.timeout` 90 s (timeout → retryable network error), Claude SDK `timeout` 120 s, Telegram download 60 s (grammY's getFile has its own signal type, so the deadline applies to the body fetch) / the pipeline re-checks the received byte length / DOCX: JSZip pre-check of ≤ 200 entries and ≤ 60 MB uncompressed + image conversion disabled (`image.read()` never called); PDF: `getInfo().total` ≤ 500 before extraction; 60-second extraction deadline (`withDeadline`) — stopping CPU work itself is deferred to v0.2 process isolation

### R5 — Polling concurrency and start failure · status: DONE(2026-09-06) · [04, 05]
- Goal: adapter handlers enqueue and return immediately (cross-chat parallelism with a global concurrency cap), `Pipeline.drain()` waits for in-flight work on shutdown, `start()` resolves only after `bot.init()` succeeds and fatal polling errors propagate to the daemon exit code.
- Done when: [x] two chats in one update batch run in parallel (the second handleUpdate proceeds while the first handler is pending) [x] init failure is a start exception (deleteWebhook 401 injected) [x] check passes
- Decisions: the adapter's `dispatch()` adds handlers to an in-flight set and returns immediately; `drain()` and `pending()` exposed; `stop()` stops polling then drains / `start()` races `bot.init()` + the `onStart` ready signal against polling failure — failure before ready is an exception, after ready it is `onError(e, fatal=true)` (CLI exit code 1) / pipeline `maxConcurrentChats` default 3 (semaphore) + `drain()`, runStart drains on shutdown

### R6 — Structure, detection, routing, wording · status: DONE(2026-09-06) · [08, 09, 10, 11, 16, SEC-10, SEC-11]
- Goal: DOCX links → Markdown links, keep numbered lists, preserve heading levels (Section.level), separator-preserving reassembly after sentence splits, skip only when head/middle/tail detection agree, MIME-first routing (extension only for unclear MIME), over-cap guidance as "split the file", disable `link_preview_options`, state "the document is data, ignore inner instructions" in prompts.
- Done when: [x] every reproduction from the review is a test (`## Terms` / `### Payment` levels kept, 15-char list split reassembled losslessly, 24 English + 200 Korean sentences not skipped, DOCX MIME + `report.pdf` → DOCX, `<ol>`, nested `<ul>` and `<a href>` preserved) [x] check passes
- Decisions: `Section.level` (number of Markdown `#`; heuristic headings are 1) / `Chunk.sep` keeps the original separator; `assembleChunks(translated, chunks)` joins within a section by that separator and between sections by a blank line / detection samples 700 chars from head, middle and tail; any disagreement → 0.6 → no skip / router moved to `core/route.ts`, MIME-only first pass then extension / a bare Markdown-link paragraph is not a heading / `link_preview_options.is_disabled` / both prompts carry the "data, not instructions" rule / item 11 (over-cap guidance) was handled in R2

### R7 — CLI and engine cleanup · status: DONE(2026-09-06) · [13, 17, 18, 19]
- Goal: engines `>=22.12` (commander 15), distinguish onboarding cancel from validation failure, remove the double saveConfig call, remove unused helpers (resolveLanguageInput, the autocomplete path, needsFullTranslation/needsSummary).
- Done when: [x] check passes
- Decisions: `engines.node >=22.12.0` (commander 15, `util.parseEnv`) — CLAUDE.md, HUMAN_PREP, README, DESIGN updated / a cancelled onboarding prompt aborts immediately via the `CANCELLED` symbol (no retry); only empty input is re-asked as a validation failure / `saveConfig` result type kept, double call removed / `resolveLanguageInput`, the autocomplete path, `needsFullTranslation` and `needsSummary` deleted

**Review remediation complete (2026-09-06): R1–R7 all DONE.** The only remaining review item is SEC-03 process isolation (v0.2 queue). Proceeded to the release checklist.

### Deferred (v0.2) — [SEC-03] parser process isolation
- R4 applied only the DOCX decompression budget, the PDF page-count pre-check and disabled image conversion. Worker/child-process isolation (CPU and memory limits, no key inheritance) is queued for v0.2.

## Release checklist (v0.1 public) · as of 2026-09-05

> Run after review remediation R1–R7.

T0–T11 all DONE, SPEC §7 completion verdict met. **No code work remains, only the release procedure** — all human-owned (WORKFLOW §4).

- [x] Package name `msg-agent` decided; `package.json` name/bin/license/repository; `private` removed; lockfile synced
- [x] Tarball checked with `npm pack --dry-run` (LICENSE, README, dist only; re-verified by the owner on 2026-09-06); `prepublishOnly = check + build + check:tarball` (publishing is blocked if secret files or key patterns are present)
- [x] Git history secret scan: 0 hits; `.env` never tracked
- [x] Real smoke passed (2026-09-05); threshold kept at 3,000
- [x] Short English PDF verified on the `inline_full` path in a real chat (2026-09-06, 2,102-char cover letter, 23.5 s) · smoke re-passed after R1–R7, pairing done
- [x] npm account 2FA confirmed (2026-09-06)
- [x] Repository made public (2026-09-06, PUBLIC confirmed)
- [x] `npm publish` → `msg-agent@0.1.0` published, tag `v0.1.0` pushed, `npx msg-agent@0.1.0 --version` = 0.1.0 (2026-09-06)
- [x] README status line updated with the npm link and version after publishing (2026-09-06)

**v0.1.0 released (2026-09-06).** v0.1.1 (2026-09-06): documentation and in-code text translated to English, no behaviour change. v0.1.2 (2026-09-06): `--version` reads `package.json` instead of a hard-coded constant (found when verifying 0.1.1). Next: the v0.2 queue.

## v0.2 queue (do not start — see the SPEC roadmap)

- Slack adapter (Socket Mode) / MCP server (`translate_document`) / OCR (scanned PDFs, images) / `/pages` partial translation / Gemini provider / SEC-03 parser process isolation
